<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <title>منشئ السيرة الذاتية</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      font-family: 'Cairo', sans-serif;
      background-color: #f5f7fa;
      margin: 0;
      padding: 0;
    }

    h1 {
      background-color: #007bff;
      color: white;
      padding: 15px;
      text-align: center;
      margin: 0;
    }

    .container {
      max-width: 800px;
      margin: 20px auto;
      background: white;
      padding: 20px;
      border-radius: 12px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }

    label {
      display: block;
      margin-top: 10px;
      font-weight: bold;
    }

    input, textarea {
      width: 100%;
      padding: 8px;
      margin-top: 5px;
      border-radius: 8px;
      border: 1px solid #ccc;
    }

    input[type="file"] {
      border: none;
    }

    button {
      background-color: #007bff;
      color: white;
      border: none;
      padding: 10px 20px;
      margin-top: 15px;
      border-radius: 8px;
      cursor: pointer;
    }

    button:hover {
      background-color: #0056b3;
    }

    #cv-preview {
      display: none;
      margin-top: 30px;
      background-color: white;
      padding: 30px;
      border-radius: 12px;
      border: 10px solid transparent;
      background-image: linear-gradient(white, white), radial-gradient(circle at top left, #007bff, #00bcd4);
      background-origin: border-box;
      background-clip: content-box, border-box;
    }

    .cv-header {
      text-align: center;
      border-bottom: 2px solid #007bff;
      margin-bottom: 20px;
      padding-bottom: 10px;
    }

    .cv-header img {
      width: 120px;
      height: 120px;
      border-radius: 50%;
      object-fit: cover;
      border: 3px solid #007bff;
    }

    .cv-section {
      margin-top: 20px;
    }

    .cv-section h3 {
      color: #007bff;
      border-bottom: 1px solid #007bff;
      padding-bottom: 5px;
    }
  </style>
</head>
<body>

<h1>منشئ السيرة الذاتية</h1>

<div class="container">
  <label>📸 الصورة الشخصية:</label>
  <input type="file" id="photo" accept="image/*">

  <label>الاسم الكامل:</label>
  <input type="text" id="name" placeholder="اكتب اسمك الكامل">

  <label>المسمى الوظيفي:</label>
  <input type="text" id="title" placeholder="مثال: مطور تطبيقات ويب">

  <label>نبذة مختصرة:</label>
  <textarea id="summary" rows="3" placeholder="اكتب فقرة قصيرة عن نفسك..."></textarea>

  <label>الخبرات العملية:</label>
  <textarea id="experience" rows="3" placeholder="اكتب خبراتك العملية..."></textarea>

  <label>المؤهلات التعليمية:</label>
  <textarea id="education" rows="3" placeholder="اكتب مؤهلاتك التعليمية..."></textarea>

  <label>المهارات:</label>
  <textarea id="skills" rows="2" placeholder="اكتب مهاراتك..."></textarea>

  <button onclick="generateCV()">عرض السيرة الذاتية</button>
  <button id="downloadBtn" style="display:none;" onclick="downloadPDF()">📄 تحميل PDF</button>

  <div id="cv-preview"></div>
</div>

<script src="https://cdnjs.cloudflare.com/ajax/libs/html2pdf.js/0.10.1/html2pdf.bundle.min.js"></script>

<script>
  let uploadedImage = "";

  // حفظ الصورة
  document.getElementById("photo").addEventListener("change", function(e) {
    const file = e.target.files[0];
    if (file) {
      const reader = new FileReader();
      reader.onload = function(evt) {
        uploadedImage = evt.target.result;
      };
      reader.readAsDataURL(file);
    }
  });

  // توليد السيرة
  function generateCV() {
    const name = document.getElementById("name").value;
    const title = document.getElementById("title").value;
    const summary = document.getElementById("summary").value;
    const experience = document.getElementById("experience").value;
    const education = document.getElementById("education").value;
    const skills = document.getElementById("skills").value;

    const cv = document.getElementById("cv-preview");

    cv.innerHTML = `
      <div class="cv-header">
        ${uploadedImage ? `<img src="${uploadedImage}" alt="صورة">` : ''}
        <h2>${name}</h2>
        <h4>${title}</h4>
      </div>
      <div class="cv-section">
        <h3>نبذة</h3>
        <p>${summary.replace(/\n/g, "<br>")}</p>
      </div>
      <div class="cv-section">
        <h3>الخبرات العملية</h3>
        <p>${experience.replace(/\n/g, "<br>")}</p>
      </div>
      <div class="cv-section">
        <h3>المؤهلات التعليمية</h3>
        <p>${education.replace(/\n/g, "<br>")}</p>
      </div>
      <div class="cv-section">
        <h3>المهارات</h3>
        <p>${skills}</p>
      </div>
    `;

    cv.style.display = "block";
    document.getElementById("downloadBtn").st
